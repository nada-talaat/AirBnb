@model IEnumerable< Airbnb.Models.Hotel>;


@{
    ViewData["Title"] = "Home Page";
}
@{
    var index = 0;
    
}
<form asp-action="Search" method="post" class="header_search">
    <input class="text-search" placeholder="Hotel" asp-controller="Home" asp-action="Search" name="searching"></input>
    <button class="header-search  searchIcon btn " asp-controller="Home" asp-action="Search" asp-route-name="Name">
        <img src="~/photos/searchIcon.svg" alt="iconsearh">
    </button>
</form>


@*Hotel Category*@
<form asp-action="category">
<div class="container text-center" style="margin-bottom:30px">
<div class="btn-group" role="group" aria-label="Basic example">
        <button type="button" class="btn btn-secondary" style="background-color:#F12954" ><a asp-action="category" style="text-decoration:none;color:inherit;"    asp-route-cid="1" >Top Rated </a> </button>
            <button type="button" class="btn btn-secondary" style="background-color:#F12954"><a asp-action="category" style="text-decoration:none;color:inherit;" asp-route-cid="2"> Best for wedding</a>  </button>
            <button type="button" class="btn btn-secondary" style="background-color:#F12954"><a asp-action="category" style="text-decoration:none;color:inherit;" asp-route-cid="3"> best resturants </a> </button>
            <button type="button" class="btn btn-secondary" style="background-color:#F12954"><a asp-action="category" style="text-decoration:none;color:inherit;" asp-route-cid="4"> romanic weekends </a> </button>
            <button type="button" class="btn btn-secondary" style="background-color:#F12954"><a asp-action="category" style="text-decoration:none;color:inherit;" asp-route-cid="5"> family friendly  </a></button>
            <button type="button" class="btn btn-secondary" style="background-color:#F12954"><a asp-action="category" style="text-decoration:none;color:inherit;" asp-route-cid="6">honeymoons    </a>  </button>
            <button type="button" class="btn btn-secondary" style="background-color:#F12954"><a asp-action="category" style="text-decoration:none;color:inherit;" asp-route-cid="7">  pet friendly  </a> </button>
            <button type="button" class="btn btn-secondary" style="background-color:#F12954"><a asp-action="category" style="text-decoration:none;color:inherit;" asp-route-cid="8"> best spa   </a>  </button>

</div>
</div>
</form>

<div class="container text-center">
    <div class="row row-cols-4"> 
        @foreach (var item in Model)
        {
            <a asp-controller="DetailsView"  asp-route-id="@item.ID" style="text-decoration:none;color:inherit;">
                <div class="col">

                    <div id="carouselExampleControls-@index" class="carousel slide rounded shadow " data-ride="false">

                    <div class="carousel-inner" >
                        @foreach (var x in item.Images)
                        {
                            <div  class="carousel-item @(x==item.Images.First()?"active":"")" >
                                    <img src=@x.img class="d-block w-100 img-thumbnail" alt="..." >
                            </div>
                        }
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls-@index" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls-@index" data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Next</span>
                    </button>
                </div>
                
                <h6>@Html.DisplayFor(modelItem=>item.Name)</h6>
                <p>@Html.DisplayFor(modelItem=>item.Rate)</p>

                @foreach (var x in item.Rooms)
                {
                    <p> @Math.Floor((decimal)x.Price) L.E</p>
                    break;
                }
                
                
            </div>
                </a>
            index++;
            
        }
      
        
        
       
    </div>
    
</div>